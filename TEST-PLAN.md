# Smart Illustrator 测试计划

**版本：** v1.0.0
**测试目标：** 开源发布前的全面功能验证
**测试环境：** macOS + Bun + Claude Code

---

## 测试概览

| 模块 | 测试数量 | 优先级 |
|------|---------|--------|
| 1. 核心模式测试 | 6 | P0 |
| 2. 风格复用功能 | 6 | P0 |
| 3. 参数组合测试 | 5 | P1 |
| 4. 引擎选择测试 | 4 | P1 |
| 5. 边界情况测试 | 4 | P2 |

**总计：** 25 个测试用例

---

## 测试前准备

### 1. 确认环境

```bash
# 检查 Bun
bun --version

# 检查 Mermaid CLI
mmdc --version

# 检查 Gemini API Key
echo $GEMINI_API_KEY
```

### 2. 准备测试文件

使用 `/tmp/smart-illustrator-test/` 作为测试目录：

```
/tmp/smart-illustrator-test/
├── articles/
│   ├── simple-article.md      # 简单文章（500 字）
│   ├── long-article.md        # 长文章（3000+ 字）
│   ├── technical-article.md   # 技术文章（多流程/架构描述）
│   └── creative-article.md    # 创意文章（多隐喻/场景描述）
├── refs/
│   ├── style-ref-01.png       # 参考图 1
│   └── style-ref-02.png       # 参考图 2
└── output/                    # 测试输出目录
```

---

## 模块 1：核心模式测试 (P0)

### Test 1.1: 文章配图模式 - 基本流程

**目的：** 验证默认模式能正确分析文章并生成配图

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md
```

**预期结果：**
- [ ] 正确分析文章结构
- [ ] 识别 2-3 个配图位置
- [ ] 生成 Mermaid 代码块（结构化内容）或 Gemini 图片（创意内容）
- [ ] 生成封面图（`-cover.png`）
- [ ] 输出 `simple-article-image.md`

---

### Test 1.2: 文章配图模式 - 无封面图

**目的：** 验证 `--no-cover` 参数

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md --no-cover
```

**预期结果：**
- [ ] 不生成封面图
- [ ] 其他配图正常生成
- [ ] `simple-article-image.md` 无封面图引用

---

### Test 1.3: PPT/Slides 模式

**目的：** 验证批量信息图 JSON 生成

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md --mode slides
```

**预期结果：**
- [ ] 输出 JSON 格式（不是 PNG）
- [ ] JSON 包含 `instruction`, `batch_rules`, `style`, `pictures` 字段
- [ ] `pictures` 数组包含多个图片定义
- [ ] `picture_1` 是封面图，包含正确的层级结构
- [ ] `style` 字段包含完整的 style prompt

---

### Test 1.4: Cover 模式 - YouTube

**目的：** 验证 YouTube 封面图生成

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md --mode cover --platform youtube
```

**预期结果：**
- [ ] 生成 16:9 横版图片
- [ ] 使用 `style-cover.md` 风格
- [ ] 输出文件名包含 `-cover-youtube.png`
- [ ] 图片质量符合 YouTube 缩略图标准

---

### Test 1.5: Cover 模式 - 微信公众号

**目的：** 验证微信公众号封面图生成

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md --mode cover --platform wechat
```

**预期结果：**
- [ ] 生成 2.35:1 比例图片（900×383）
- [ ] 输出文件名包含 `-cover-wechat.png`

---

### Test 1.6: Cover 模式 - 直接指定主题

**目的：** 验证无需文章直接生成封面

**测试命令：**
```bash
/smart-illustrator --mode cover --platform youtube --topic "Claude 4 深度评测"
```

**预期结果：**
- [ ] 无需文章路径
- [ ] 根据 `--topic` 生成封面图
- [ ] 使用深色科技风格

---

## 模块 2：风格复用功能测试 (P0)

### Test 2.1: 保存项目级配置

**目的：** 验证 `--save-config` 正确保存配置

**测试命令：**
```bash
cd /tmp/smart-illustrator-test/articles
/smart-illustrator simple-article.md --ref ../refs/style-ref-01.png --save-config
```

**预期结果：**
- [ ] 生成配图（带参考图影响）
- [ ] 创建 `.smart-illustrator/config.json`
- [ ] 配置文件包含相对路径的 `references`
- [ ] 路径相对于项目根目录（不是 `.smart-illustrator/` 目录）

**验证配置文件：**
```bash
cat /tmp/smart-illustrator-test/articles/.smart-illustrator/config.json
# 应该看到：
# {
#   "references": ["../refs/style-ref-01.png"]
# }
```

---

### Test 2.2: 保存用户级配置

**目的：** 验证 `--save-config-global` 正确保存到用户目录

**测试命令：**
```bash
/smart-illustrator /tmp/smart-illustrator-test/articles/simple-article.md --ref /tmp/smart-illustrator-test/refs/style-ref-02.png --save-config-global
```

**预期结果：**
- [ ] 创建 `~/.smart-illustrator/config.json`
- [ ] 配置文件包含 `references` 字段

**验证：**
```bash
cat ~/.smart-illustrator/config.json
```

---

### Test 2.3: 配置自动加载

**目的：** 验证配置文件被自动加载

**测试命令：**
```bash
cd /tmp/smart-illustrator-test/articles
/smart-illustrator simple-article.md
# 不指定 --ref，应该自动使用配置中的参考图
```

**预期结果：**
- [ ] 控制台显示 `✓ Loaded project config: ...`
- [ ] 自动使用配置文件中的参考图
- [ ] 生成的图片风格符合参考图

---

### Test 2.4: 配置优先级 - CLI 覆盖配置文件

**目的：** 验证 CLI 参数优先于配置文件

**测试命令：**
```bash
cd /tmp/smart-illustrator-test/articles
/smart-illustrator simple-article.md --ref ../refs/style-ref-02.png
# 配置文件是 ref-01，CLI 指定 ref-02
```

**预期结果：**
- [ ] 使用 CLI 指定的 `style-ref-02.png`
- [ ] 不使用配置文件中的 `style-ref-01.png`

---

### Test 2.5: --no-config 禁用配置

**目的：** 验证 `--no-config` 完全忽略配置文件

**测试命令：**
```bash
cd /tmp/smart-illustrator-test/articles
/smart-illustrator simple-article.md --no-config
```

**预期结果：**
- [ ] 控制台**不显示** `✓ Loaded ... config`
- [ ] 不使用任何参考图
- [ ] 使用默认风格生成

---

### Test 2.6: 配置优先级 - 项目级覆盖用户级

**准备：** 确保项目级和用户级配置都存在，且参考图不同

**测试命令：**
```bash
cd /tmp/smart-illustrator-test/articles
/smart-illustrator simple-article.md
```

**预期结果：**
- [ ] 显示加载了两个配置
- [ ] 使用项目级配置的参考图（覆盖用户级）

---

## 模块 3：参数组合测试 (P1)

### Test 3.1: --style 风格选择

**测试命令：**
```bash
/smart-illustrator article.md --style dark
/smart-illustrator article.md --style minimal
/smart-illustrator article.md --style light  # 默认
```

**预期结果：**
- [ ] `--style dark` 使用深色科技风格
- [ ] `--style minimal` 使用极简风格
- [ ] 风格正确应用到生成的图片

---

### Test 3.2: --candidates 候选图数量

**测试命令：**
```bash
/smart-illustrator article.md -c 2
```

**预期结果：**
- [ ] 生成 2 张候选图
- [ ] 输出文件名包含 `-1.png`, `-2.png`
- [ ] 控制台显示 `Quality Router: 2 candidates generated`

---

### Test 3.3: --prompt-only 只输出 Prompt

**测试命令：**
```bash
/smart-illustrator article.md --prompt-only
```

**预期结果：**
- [ ] 输出 Prompt 文本
- [ ] **不调用** API 生成图片
- [ ] **不创建** PNG 文件

---

### Test 3.4: --ref 多张参考图

**测试命令：**
```bash
/smart-illustrator article.md --ref ref1.png --ref ref2.png --ref ref3.png
```

**预期结果：**
- [ ] 正确解析 3 张参考图
- [ ] 全部传递给 Gemini API
- [ ] 生成的图片融合多张参考图风格

---

### Test 3.5: --list-styles 列出可用风格

**测试命令：**
```bash
/smart-illustrator --list-styles
```

**预期结果：**
- [ ] 列出 `styles/` 目录下所有可用风格
- [ ] 显示：light, dark, minimal, cover

---

## 模块 4：引擎选择测试 (P1)

### Test 4.1: Mermaid 引擎 - 流程图

**测试文章：** 包含明确流程/步骤描述的段落

**预期结果：**
- [ ] 自动选择 Mermaid 引擎
- [ ] 生成 `flowchart` 代码块
- [ ] 代码块直接嵌入文章（不是 PNG）

---

### Test 4.2: Mermaid 引擎 - 架构图

**测试文章：** 包含系统组件/层次结构描述

**预期结果：**
- [ ] 自动选择 Mermaid 引擎
- [ ] 生成 `block-beta` 或 `flowchart` 代码块

---

### Test 4.3: Gemini 引擎 - 概念图

**测试文章：** 包含抽象概念解释

**预期结果：**
- [ ] 自动选择 Gemini 引擎
- [ ] 调用 Gemini API 生成 PNG
- [ ] 图片风格符合指定的 style

---

### Test 4.4: Gemini 引擎 - 隐喻图

**测试文章：** 包含"像...一样"、"比喻"等表达

**预期结果：**
- [ ] 自动选择 Gemini 引擎
- [ ] 生成创意视觉隐喻图

---

## 模块 5：边界情况测试 (P2)

### Test 5.1: 空文章/短文章

**测试文章：** 少于 100 字

**预期结果：**
- [ ] 不生成配图（或仅生成封面图）
- [ ] 给出合理提示

---

### Test 5.2: 超长文章

**测试文章：** 5000+ 字

**预期结果：**
- [ ] 正确处理，不超时
- [ ] 配图数量控制在合理范围（4-6 张）

---

### Test 5.3: 配置文件不存在

**测试场景：** 删除所有配置文件后运行

**预期结果：**
- [ ] 不报错
- [ ] 使用默认设置继续
- [ ] 正常生成图片

---

### Test 5.4: API 错误处理

**测试场景：** 使用无效的 API Key 或网络断开

**预期结果：**
- [ ] 显示清晰的错误信息
- [ ] 不崩溃
- [ ] Mermaid 图表仍能正常生成

---

## 测试执行记录

| 测试 ID | 测试名称 | 状态 | 备注 |
|---------|---------|------|------|
| 1.1 | 文章配图 - 基本流程 | ⏳ | |
| 1.2 | 文章配图 - 无封面图 | ⏳ | |
| 1.3 | PPT/Slides 模式 | ⏳ | |
| 1.4 | Cover - YouTube | ⏳ | |
| 1.5 | Cover - 微信 | ⏳ | |
| 1.6 | Cover - 直接指定主题 | ⏳ | |
| 2.1 | 保存项目级配置 | ⏳ | |
| 2.2 | 保存用户级配置 | ⏳ | |
| 2.3 | 配置自动加载 | ⏳ | |
| 2.4 | CLI 覆盖配置 | ⏳ | |
| 2.5 | --no-config 禁用 | ⏳ | |
| 2.6 | 项目级覆盖用户级 | ⏳ | |
| 3.1 | --style 风格选择 | ⏳ | |
| 3.2 | --candidates 候选图 | ⏳ | |
| 3.3 | --prompt-only | ⏳ | |
| 3.4 | --ref 多张参考图 | ⏳ | |
| 3.5 | --list-styles | ⏳ | |
| 4.1 | Mermaid - 流程图 | ⏳ | |
| 4.2 | Mermaid - 架构图 | ⏳ | |
| 4.3 | Gemini - 概念图 | ⏳ | |
| 4.4 | Gemini - 隐喻图 | ⏳ | |
| 5.1 | 空/短文章 | ⏳ | |
| 5.2 | 超长文章 | ⏳ | |
| 5.3 | 配置不存在 | ⏳ | |
| 5.4 | API 错误处理 | ⏳ | |

**状态说明：**
- ⏳ 待测试
- ✅ 通过
- ❌ 失败
- 🔄 需要复测

---

## 测试完成标准

**开源发布前必须满足：**

1. **P0 测试 100% 通过**（核心模式 + 风格复用）
2. **P1 测试 ≥90% 通过**（参数组合 + 引擎选择）
3. **P2 测试 ≥80% 通过**（边界情况）
4. **无阻塞性 Bug**

---

**测试计划创建时间：** 2026-01-30
**预计测试时间：** 1-2 小时
